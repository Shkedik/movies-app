export enum GenreType {
  MOVIE = 'movie',
  SERIES = 'series',
  NONE = 'Select type',
}

export type ContentType = {
  name: string,
  title: string,
  logo: string
}

export enum FilteredEnum {
  TEXT = 'text',
  NUMBER = 'number'
}
