export enum GenreType {
  NONE = 'Select type',
  MOVIE = 'movie',
  SERIES = 'series',
}

export type ContentType = {
  name: string,
  title: string,
  logo: string
}

export enum FilteredEnum {
  TEXT = 'text',
  NUMBER = 'number'
}
